/**
 * Implementation of hook_block
 */
function HOOK_block($op, $delta = 0, $edit = array()) {
  switch($op) {
    // Block view options
    case 'list':
      $blocks[**] = array(
        'info' => t(**)
      );
      return $blocks;
    break;
    case 'view':
      switch($delta) {
        case **:
          $block['subject'] = **;
          $block['content'] = **;
        break;
      }
      return $block;
    break;

    // Configuration options
    case 'configure':
    break;
    case 'save':
    break;
  }
}
