/**
 * Implements hook_user().
 */
function HOOK_user($op, &$edit, &$account, $category = NULL) {
  switch($op) {
    // after_update, categories, delete, form, insert, load,
    // login, logout, register, submit, update, validate, view
  }
}
